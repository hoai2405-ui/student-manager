# Copy file này thành .env và chỉnh lại cho máy của bạn
# (Không commit secret thật)

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=shlx
DB_PORT=3306

# Bắt buộc set khi deploy production
JWT_SECRET=change-me
